[package]
name = "scanner-analysis"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[dependencies]
scanner-rules = { path = "../scanner-rules" }
scanner-repository = { path = "../scanner-repository" }
strsim = "0.11"
thiserror = "2"
regex = "1"
serde_json = "1"
